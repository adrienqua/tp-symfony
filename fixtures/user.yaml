App\Entity\User:
    user_template (template):
        email: "<email()>"
        username: "<userName()>"
        password: <hashPassword("user<autoIncrement()>")>
        roles: [ROLE_USER]

    user_admin (extends user_template):
        email: "adrien.quacchia@gmail.com"
        username: "adrienqua"
        password: <hashPassword("adrien1234")>
        roles: [ROLE_ADMIN]

    user_with_subscription_{1..5} (extends user_template):
        email: "<email()>"
        currentSubscriptionId: "@subscription_*"

    user_without_subscription_{1..5} (extends user_template):
